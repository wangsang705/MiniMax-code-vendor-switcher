import { VendorInstance } from '../api';

export function VendorDialog({
  editing,
  onClose,
  onSaved,
}: {
  editing: VendorInstance | null;
  onClose: () => void;
  onSaved: () => void;
}) {
  // TODO: Task 14 - 完整实现
  void editing;
  void onClose;
  void onSaved;
  return null;
}
